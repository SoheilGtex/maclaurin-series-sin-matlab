# Results

These results were generated by the repository's MATLAB reproduction pipeline and reviewed before being committed.

Execution environment for the committed reference set:

- MATLAB R2026a Update 5
- Platform: `GLNXA64`
- Execution: GitHub Actions
- Test suite: passed
- Full reproduction pipeline: passed

The directory structure separates evidence by purpose:

- `raw/`: machine- and release-specific CSV outputs generated by the experiment driver; not committed.
- `reference/`: selected generated CSV summaries retained for inspection and comparison.
- `figures/`: reviewed generated PNG figures used to visualize convergence, approximation error, floating-point effects, range reduction, and accuracy-runtime trade-offs.

Run from the repository root in MATLAB:

```matlab
addpath('src');
addpath('experiments');
generateAllFigures;
```

The committed reference files are generated outputs rather than hand-entered numerical results. Runtime measurements remain environment-specific and should be regenerated when comparing performance on a different MATLAB release or platform.
